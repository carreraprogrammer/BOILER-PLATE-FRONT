import { Link } from 'react-router-dom'; import { AuthLayout } from '../../templates/AuthLayout'; import { Spinner } from '../../atoms/Spinner'; import { DynamicForm } from '../../organisms/DynamicForm'; import { useFormStore } from '../../../store/formStore'; import { useEffect } from 'react';
const fallbackSchema={ slug:'login-form', title:'Login', submit_endpoint:'/api/v1/auth/login', submit_method:'POST' as const, fields:[{name:'email', type:'email' as const, label:'Email'},{name:'password', type:'password' as const, label:'Password'}] };
export const LoginPage=()=>{ const schema=useFormStore((state)=>state.schemas['login-form']); const isLoading=useFormStore((state)=>state.isLoading); const fetchSchema=useFormStore((state)=>state.fetchSchema); useEffect(()=>{ void fetchSchema('login-form').catch(()=>undefined); },[fetchSchema]); if(isLoading && !schema) return <Spinner />; return <AuthLayout title='Iniciar sesión'><DynamicForm schema={schema ?? fallbackSchema} /><Link to='/register'>¿No tienes cuenta? Regístrate</Link></AuthLayout>; };
