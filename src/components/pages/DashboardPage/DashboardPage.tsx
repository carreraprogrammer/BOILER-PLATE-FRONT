import { AppLayout } from '../../templates/AppLayout'; import { useAuthStore } from '../../../store/authStore'; import { PermissionGate } from '../../atoms/PermissionGate'; import { EmptyState } from '../../molecules/EmptyState';
export const DashboardPage=()=>{ const user=useAuthStore((state)=>state.user); const hasAdminLinks=Boolean(user?.superAdmin || user?.permissions.some((permission)=>['users:read','roles:read','forms:read'].includes(permission))); return <AppLayout title='Dashboard'><p>{`Hola, ${user?.name ?? 'Usuario'}`}</p><p>{user?.email}</p><PermissionGate permission='users:read'><div><h2>Permisos activos</h2><ul>{user?.permissions.map((permission)=><li key={permission}>{permission}</li>)}</ul></div></PermissionGate>{!hasAdminLinks ? <EmptyState message='No tienes módulos administrativos asignados.' /> : null}</AppLayout>; };
