import { AppLayout } from '../../templates/AppLayout'; import { DynamicForm } from '../../organisms/DynamicForm'; import { useAuthStore } from '../../../store/authStore';
export const ProfilePage=()=>{ const user=useAuthStore((state)=>state.user); return <AppLayout title='Mi perfil'><DynamicForm schema={{ slug:'profile-form', title:'Perfil', submit_endpoint:`/api/v1/users/${user?.id ?? ':id'}`, submit_method:'PATCH', fields:[{ name:'name', type:'text', label:'Nombre' },{ name:'email', type:'email', label:'Email' }] }} initialValues={{ name:user?.name ?? '', email:user?.email ?? '' }} /></AppLayout>; };
