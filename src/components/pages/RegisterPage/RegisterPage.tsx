import { Link } from 'react-router-dom'; import { AuthLayout } from '../../templates/AuthLayout'; import { DynamicForm } from '../../organisms/DynamicForm'; import { GoogleButton } from '../../atoms/GoogleButton'; import styles from './RegisterPage.module.css';
const schema={ slug:'register-form', title:'Register', submit_endpoint:'/api/v1/auth/register', submit_method:'POST' as const, fields:[{name:'name', type:'text' as const, label:'Nombre'},{name:'email', type:'email' as const, label:'Email'},{name:'password', type:'password' as const, label:'Password'}] };
export const RegisterPage=()=> <AuthLayout title='Registro'><GoogleButton onClick={()=>{ window.location.assign(`${import.meta.env.VITE_API_URL ?? ''}/api/v1/auth/google`); }} /><div className={styles.divider} aria-hidden='true'><span className={styles.line} /><span className={styles.dividerText}>o regístrate con tu email</span><span className={styles.line} /></div><DynamicForm schema={schema} /><Link to='/login'>¿Ya tienes cuenta? Inicia sesión</Link></AuthLayout>;
